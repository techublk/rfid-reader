<div class="container">
        <div class="row">
            <div class="col text-center">
                <svg xmlns="http://www.w3.org/2000/svg" width="160" height="160" fill="currentColor" class="status-icon {{ $c0 === 1 ? 'green' : ($c0 === 0 ? 'red' : '') }} bi bio bi-circle-fill" viewBox="0 0 16 16">
                <circle cx="8" cy="8" r="8"/>
                </svg>
            </div>
            <div class="col text-center">
                <svg xmlns="http://www.w3.org/2000/svg" width="160" height="160" fill="currentColor" class="status-icon {{ $c1 === 1 ? 'green' : ($c1 === 0 ? 'red' : '') }} bi bi-circle-fill" viewBox="0 0 16 16">
                <circle cx="8" cy="8" r="8"/>
                </svg>
            </div>
            <div class="col text-center">
                <svg xmlns="http://www.w3.org/2000/svg" width="160" height="160" fill="currentColor" class="status-icon {{ $c2 === 1 ? 'green' : ($c2 === 0 ? 'red' : '') }} bi bi-circle-fill" viewBox="0 0 16 16">
                <circle cx="8" cy="8" r="8"/>
                </svg>
            </div>
            <<div class="col text-center">
                <svg xmlns="http://www.w3.org/2000/svg" width="160" height="160" fill="currentColor" class="status-icon {{ $c3 === 1 ? 'green' : ($c3 === 0 ? 'red' : '') }} bi bi-circle-fill" viewBox="0 0 16 16">
                <circle cx="8" cy="8" r="8"/>
                </svg>
            </div>
        </div>
    </div>